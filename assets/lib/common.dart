import 'package:flutter/material.dart';

const bg = Color(0xFF05070B);
const card = Color(0xFF0C0F14);

BoxDecoration cardDeco() => BoxDecoration(
  color: card,
  borderRadius: BorderRadius.circular(18),
  border: Border.all(color: Colors.white12),
);
